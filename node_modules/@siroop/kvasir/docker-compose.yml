version: '2'

services:
  kvasir:
    container_name: "kvasir"
    build: .
    volumes:
      - ./source:/usr/src/app/source
      - ./public:/usr/src/app/public
    ports:
      - "3000:3000"

networks:
  default:
    external:
      name: siroop
