const componentsContext = require.context('../source/_assets/', true, /\.js?$/);

componentsContext.keys().forEach(componentsContext);

const testsContext = require.context('../source/', true, /.*?\.spec\.js$.*/);

testsContext.keys().forEach(testsContext);
