module.exports = (gulp, plugins, options) => () => {
	const stream = gulp.src(options.paths.videos.src)
		.pipe(plugins.changed(options.paths.videos.dest))
		.pipe(gulp.dest(options.paths.videos.dest));

	return stream;
};
