module.exports = (gulp, plugins, options) => () => {
	const stream = gulp.src(options.paths.images.src)
		.pipe(plugins.changed(options.paths.images.dest))
		.pipe(gulp.dest(options.paths.images.dest));

	return stream;
};
