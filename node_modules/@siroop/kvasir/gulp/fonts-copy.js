module.exports = (gulp, plugins, options) => () => {
	const stream = gulp.src(options.paths.fonts.src)
		.pipe(plugins.changed(options.paths.fonts.dest))
		.pipe(gulp.dest(options.paths.fonts.dest));

	return stream;
};
