const hideClass = 'js-hidden';

class BadgeInternal {
	constructor($el) {
		this.el = $el.get(0);
		this.el.addEventListener('click', e => this.onBadgeClick(e));
	}

	onBadgeClick() {
		this.el.classList.add(hideClass);
	}
}

export default BadgeInternal;
