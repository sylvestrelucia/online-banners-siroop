<textarea
	class="a-form-textarea{% if modifier %} a-form-textarea--{{ modifier }}{% endif %}"
	id="{{ data.id }}"
	name="{{ data.name }}"
	{% if options.required %} required {% endif %}
	{% if not data.placeholder is empty %}placeholder="{{ data.placeholder }}"{% endif %}
	data-c-name="FormTextarea"></textarea>
